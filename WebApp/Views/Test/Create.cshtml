@model WebApp.Models.ViewModel

@{
    ViewData["Title"] = "Test ";
}

<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="KursId" class="control-label">Kurs</label>
                <select asp-for="KursId" asp-items="Model.Kursevi">
                </select>
            </div>
            <div class="form-group">
                <label asp-for="Nivo" class="control-label"></label>
                <input asp-for="Nivo" class="form-control" />
                <span asp-validation-for="Nivo" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Unesi novi test" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
